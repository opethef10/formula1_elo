[project]
name = "formula1-elo"
version = "0.1.0"
description = "Rank the best qualifying drivers in Formula 1 history using the Elo rating system."
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "openpyxl>=3.1.5",
    "pandas>=2.3.3",
]
